# 2026-07-02

## 오늘 한 일
- 코딩테스트
    - permutations
        - https://neetcode.io/problems/permutations/question
    - valid-binary-search-tree
        - https://neetcode.io/problems/valid-binary-search-tree/question
    - jump-game
        - https://neetcode.io/problems/jump-game/question
    - unique-path
        - https://neetcode.io/problems/count-paths/question

## 정리
 - permutations
    - 간단한 백트래킹 문제
    - vector에 push_back을 한다면 참조값이 넘어가는지 복사값이 넘어가는지 헷갈렸다
        ```cpp
        // vector의 push_back
        void push_back(const T& value); // 복사
        void push_back(T&& value);      // 이동
        ```
- valid-binary-search-tree
    - 좌우 서브트리의 값을 제한한다
- jump-game
    - 간단한 dp 문제
        ```cpp
        dp[i] = max(dp[i], i + dist);
        ```
- unique-path
    - 경로의 갯수 구하기 문제, dp
        ```cpp
        dp[i][j] = dp[i-1][j] + dp[i][j-1];
        ```
